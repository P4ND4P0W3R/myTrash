type ModalProps = {
    showModalTrash: number
}

export const ModalSideTrash = (props: ModalProps) => {

    console.log(props.showModalTrash)
    return(
        <div className="sideBarContainer">
            <p>hjhb</p>
        </div>
    )
}